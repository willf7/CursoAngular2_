let variavelName = "Minha variavel";

function somar(x, y) {
  return x + y;
}

let numeros = [1, 2, 3];
numeros.map((i) => {
  return i * 2;
});

let num1: number = 7;
let userName: string = "will";
